**Контрольные вопросы к Assignment 2

(OpenMP, CUDA и гетерогенные вычисления)**

1. Что понимается под гетерогенной параллелизацией?

Гетерогенная параллелизация — это подход, при котором в одной задаче одновременно используются разные вычислительные устройства, чаще всего CPU и GPU. При этом каждая часть задачи выполняется на том устройстве, которое лучше всего для неё подходит. CPU обычно управляет логикой и последовательными участками, а GPU — массовыми параллельными вычислениями.

2. В чём принципиальные различия архитектур CPU и GPU?

CPU имеет небольшое количество мощных ядер, оптимизированных для сложной логики, ветвлений и последовательного выполнения. GPU, наоборот, содержит тысячи более простых ядер, рассчитанных на выполнение одинаковых операций над большими объёмами данных.
Проще говоря, CPU — универсальный и гибкий, GPU — специализированный и массово-параллельный.

3. Какие типы задач лучше подходят для выполнения на GPU, а какие — на CPU?

GPU лучше подходит для задач с высокой степенью параллелизма, где одна и та же операция выполняется над большим массивом данных (сортировка, обработка изображений, матричные операции).
CPU эффективнее для задач с сложной логикой, ветвлениями, рекурсией и небольшим объёмом данных, а также для управления программой в целом.

4. Почему не все алгоритмы эффективно распараллеливаются с использованием OpenMP?

Не все алгоритмы хорошо распараллеливаются из-за:

зависимостей между шагами;

необходимости синхронизации потоков;

небольшого объёма вычислений на итерацию.

Если накладные расходы на создание потоков и синхронизацию превышают выигрыш от параллелизма, производительность может даже снизиться.

5. В чём заключается основная идея алгоритма сортировки слиянием?

Сортировка слиянием основана на принципе «разделяй и властвуй».
Массив рекурсивно делится на более мелкие части, которые сортируются независимо, после чего отсортированные подмассивы сливаются в один упорядоченный массив. Алгоритм имеет стабильную сложность 
𝑂
(
𝑛
log
⁡
𝑛
)
O(nlogn).

6. Какие сложности возникают при реализации сортировки слиянием на GPU?

Основные сложности связаны с:

эффективным параллельным слиянием подмассивов;

управлением памятью GPU;

синхронизацией между блоками потоков;

дополнительными затратами на копирование данных между CPU и GPU.

Также важно правильно организовать доступ к памяти, чтобы избежать потери производительности.

7. Как выбор размера блока и сетки влияет на производительность вычислений на GPU?

Размер блоков и сетки определяет, насколько полно используется GPU.
Слишком маленькие блоки приводят к недозагрузке вычислительных ядер, а слишком большие — к повышенному потреблению ресурсов и снижению эффективности.
Оптимальный выбор позволяет увеличить параллелизм и скрыть задержки доступа к памяти.

8. Почему гетерогенный подход может быть эффективнее использования только CPU или только GPU?

Гетерогенный подход позволяет использовать сильные стороны каждого устройства. CPU эффективно управляет программой и выполняет сложную логику, а GPU ускоряет вычислительно тяжёлые параллельные участки.
В результате снижается общее время выполнения и повышается производительность по сравнению с использованием только одного типа устройства.
