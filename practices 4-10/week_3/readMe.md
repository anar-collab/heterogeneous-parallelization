Контрольные вопросы
1. В чём различие между последовательной и параллельной сортировкой слиянием?

Последовательная версия выполняет сортировку и слияние поочерёдно, тогда как параллельная позволяет сортировать части массива и сливать их одновременно несколькими потоками.

2. Как распределение потоков и блоков влияет на производительность CUDA?

Неправильное распределение приводит к недоиспользованию GPU или перегрузке ресурсов. Оптимальное распределение позволяет максимально задействовать вычислительные ядра.

3. Какие сложности возникают при реализации быстрой сортировки на GPU?

Основные сложности:

рекурсивная структура алгоритма;

неравномерное распределение данных;

сложная синхронизация потоков.

4. В каких случаях GPU-сортировка может быть медленнее CPU?

при небольшом размере массива;

при частых копированиях данных между CPU и GPU;

при плохом распределении потоков.

5. Почему важно правильно выбирать размер блоков и потоков?

Размер блоков и потоков влияет на:

загрузку GPU;

использование памяти;

количество активных потоков;

общую производительность.

6. Как использование разделяемой памяти влияет на производительность сортировки?

Разделяемая память значительно быстрее глобальной и позволяет ускорить доступ к данным внутри блока, снижая задержки.

7. Что означает принцип «разделяй и властвуй» в сортировках?

Это принцип, при котором задача разбивается на более мелкие подзадачи, каждая из которых решается отдельно, а затем результаты объединяются.

📌Выводы

В ходе работы были реализованы последовательные и параллельные алгоритмы сортировки. Эксперимент показал, что при малых размерах массива использование GPU не даёт выигрыша из-за накладных расходов на передачу данных. При увеличении объёма данных параллельные алгоритмы на GPU демонстрируют лучшую производительность. Однако не все алгоритмы сортировки одинаково эффективно масштабируются на GPU, особенно быстрая и пирамидальная сортировки.